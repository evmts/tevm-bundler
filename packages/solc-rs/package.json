{
	"name": "@tevm/solc-rs",
	"version": "1.0.0-rc.151",
	"private": true,
	"description": "Rust wrapper for the Solidity compiler (solc)",
	"type": "module",
	"scripts": {
		"build": "cargo build --release",
		"build:debug": "cargo build",
		"build:release": "cargo build --release",
		"test": "cargo test",
		"typecheck": "cargo check",
		"test:run": "cargo test"
	},
	"devDependencies": {
		"@napi-rs/cli": "^3.6.2"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/evmts/tevm-bundler.git",
		"directory": "packages/solc-rs"
	},
	"publishConfig": {
		"access": "public",
		"provenance": true
	}
}
